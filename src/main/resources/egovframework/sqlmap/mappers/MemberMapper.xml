<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="egovframework.example.member.service.MemberMapper">

	<select id="loginCheck" resultType="String">
		SELECT memNm 
		FROM MEMBER
		WHERE memId = #{memId} AND memPw = #{memPw}
	</select>
	
	<select id="viewMember" resultType="egovframework.example.member.vo.MemberVo">
		SELECT 
			memNo,
			memId,
			memPw,
			memNm
		FROM MEMBER
		WHERE memId = #{memId} AND memPw = #{memPw}
	
	</select>
	
	
	
	<select id="idCheck" parameterType="String" resultType="int">
		SELECT 
			count(memId) 
		FROM MEMBER 
		WHERE mem_id = #{memId}
	</select>


	<!-- 회원가입 -->
	<insert id="join" parameterType="egovframework.example.member.vo.MemberVo">
		<selectKey resultType="int" order="BEFORE" keyProperty="memNo">
			SELECT MEMSEQ.NEXTVAL FROM DUAL
		</selectKey>
		
		INSERT INTO MEMBER (
		    memNo,
		    memId,
		    memPw,
		    memNm,
		    memMail
		) VALUES (
		    #{memNo},
		    #{memId},
		    #{memPw},
		    #{memNm},
		    #{memMail}
		)
	</insert>
	
	
	<!-- 아이디 중복 검사 -->
	<select id="memIdCheck" resultType="int">
	
		SELECT COUNT(*)
		FROM MEMBER 
		WHERE memId = #{memId}
	
	</select>
	
	

</mapper>





